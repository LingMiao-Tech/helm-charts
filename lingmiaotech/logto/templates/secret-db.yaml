# TODO: Add support for secret-db
# {{- if not .Values.externalPostgresql.existingSecret }}
# apiVersion: v1
# kind: Secret
# metadata:
#   name: {{ include "logto.fullname" . }}-db
#   labels:
#     {{- include "logto.labels" . | nindent 4 }}
# type: Opaque
# data:
#   db-url: {{ printf "postgres://%s:%s@%s:%d/%s" .Values.externalPostgresql.username .Values.externalPostgresql.password .Values.externalPostgresql.host (int .Values.externalPostgresql.port) .Values.externalPostgresql.database | b64enc }}
# {{- end }}
